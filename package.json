{
  "name": "quickfix-local",
  "version": "1.0.0",
  "description": "QuickFix AI - Local Development Environment",
  "scripts": {
    "setup": "chmod +x setup-local-dev.sh && ./setup-local-dev.sh",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "test": "cd backend && npm test && cd ../frontend && npm test",
    "clean": "rm -rf backend/node_modules backend/dist frontend/node_modules frontend/build",
    "env:check": "echo 'Checking environment files...' && ls -la backend/.env frontend/.env"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["quickfix", "ai", "home-maintenance", "local-development"],
  "author": "QuickFix Development Team",
  "license": "Private",
  "engines": {
    "node": ">=18.0.0"
  }
} 